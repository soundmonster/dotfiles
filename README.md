# dotfiles

First, do this:

```sh
pip install dotfiles
brew install zsh tmux ctags tig
# Oh My Zsh
sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
# Tmux theme pack
git clone https://github.com/jimeh/tmux-themepack.git ~/.tmux-themepack
```
